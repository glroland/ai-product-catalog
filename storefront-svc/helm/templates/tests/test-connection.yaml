apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storefront-svc.fullname" . }}-test-connection"
  labels:
    {{- include "storefront-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storefront-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
